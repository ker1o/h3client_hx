package engine;
import constants.GameConstants;
import client.maphandler.MapHandler.TFlippedCache;
import client.maphandler.MapHandler.TFlippedAnimations;
import gui.Animation;

class TextureManager {

    var _atlasPacker:AtlasPacker;

    public function new() {
        _atlasPacker = new AtlasPacker();
    }

}
